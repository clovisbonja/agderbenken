# Supabase oppsett

1. Åpne Supabase-prosjektet ditt (`qbyowsxgphyecgucthon`) og gå til SQL Editor.
2. Kjør `schema.sql`.
3. Kjør `seed.sql`.

Frontend-konfig ligger i:
- `my-app/.env.local`
- `my-app/src/lib/supabase.ts`
